export enum ImportType {
  LOCAL = 'local',
  ONLINE = 'online',
  NOTION = 'notion',
  GOOGLE = 'google',
  LARK = 'lark',
  CUSTOM = 'custom',
}

export enum ExternalTypeName {
  TEXT = 'text',
  IMAGE = 'image',
}
